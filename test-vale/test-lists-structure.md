# Test: Lists, Numbers, and Structure

Rules covered: ListStructure, Numbers, OxfordComma, Ranges

---

## Multicolumn lists (ListStructure.yml)

Avoid using a multicolumn list to display configuration options; use a table instead.

## 24x7 shorthand in context (ListStructure.yml)

The service is available 24x7 for enterprise customers.

---

## Number ranges — context (Ranges.yml)

The valid port range is 1-65535.
Response codes 400-499 indicate client errors.
The cache TTL should be between 60-3600 seconds.

---

## Oxford comma in lists (OxfordComma.yml)

To complete the setup, configure the origin, update the DNS record and activate the rule.
The platform supports caching, load balancing and traffic routing.
Assign roles to developers, administrators and auditors.

---

## Lists — single digit numerals in prose (Numbers.yml)

There are 3 steps in this process.
Choose 1 of the 4 available authentication methods.
The API returns 1 of 3 possible status codes.
Set the retry count to 5.

---

## Ranges — prefer "from" or "through" (Ranges.yml)

Values 1-9 must be spelled out in prose.
The configuration applies to ports 80-443.
