# Test: Grammar and Voice

Rules covered: Adverbs, Backend, Contractions, AkamaiContractions, OxfordComma, Passive, SentenceLength, Semicolons, Suspended, Acronyms, Vocab, Foreign, References

---

## Adverbs (Adverbs.yml)

The system easily handles high traffic.
Requests are quickly routed to the nearest edge server.
The configuration is simply done through the portal.
The feature was rapidly deployed across all regions.
The update was successfully applied to all nodes.

## Backend spelling (Backend.yml)

The backend service processes all incoming requests.
This is a back-end configuration setting.
Changes to the backend are reflected immediately.

## Contractions — use them (Contractions.yml)

The system does not support IPv6 at this time.
You should not disable this feature in production.
It is possible to configure multiple origins.
The service is not available in all regions.
If you are not sure, contact support.

## Contractions — not with Akamai brand (AkamaiContractions.yml)

Akamai's going to release this feature next quarter.
Akamai's been working on this for several months.
Akamai's done significant testing on this component.

## Oxford comma (OxfordComma.yml)

Configure the origin, update the DNS settings and test the connection.
The report includes errors, warnings and suggestions.
You can use certificates, API keys and tokens.
The system supports HTTP, HTTPS and WebSocket connections.

## Passive voice (Passive.yml)

The configuration is updated by the system automatically.
Requests are processed by the edge server.
The certificate was generated by the provisioning service.
Logs are stored by the logging pipeline for 30 days.

## Sentence length (SentenceLength.yml)

The Akamai platform provides a comprehensive suite of content delivery services that enable organizations to accelerate and secure their web applications, APIs, and media content delivery at massive global scale.

When configuring the origin server, it is important to ensure that the hostname, port, and protocol are all set correctly before you save and activate the configuration in the portal.

## Semicolons (Semicolons.yml)

Enable caching on the edge server; this reduces origin load significantly.
The API returns a 200 status code; the response body contains the token.
Configure the timeout; the default value is 30 seconds.

## Suspended hyphens (Suspended.yml)

This feature supports both short- and long-term caching strategies.
Configure the read- and write-timeout values.

## Foreign expressions (Foreign.yml)

Use the portal, e.g. the Control Center, to manage your configuration.
The service supports multiple protocols, i.e. HTTP and HTTPS.
Configure the origin, e.g. hostname and port, before activating.

## References — click here (References.yml)

Click here to view the full configuration guide.
Click here for more details about the API.

## Vocabulary check (Vocab.yml)

Ensure the configuration is correct before activating.
Insure all settings have been saved.
The above steps complete the configuration.
