# Test: Abbreviations and Acronyms

Rules covered: Abbreviations, AMPM, Acronyms, HeadingAcronyms

---

## Abbreviations — periods in acronyms (Abbreviations.yml)

The L.D.A.P. server is used for authentication.
Configure the A.P.I. endpoint before you begin.
Enable T.L.S. on all connections.
The U.R.L. must be reachable from the internet.
Edit the C.S.S. file to update the layout.
The J.S.O.N. payload is returned by the endpoint.

## Abbreviations — apostrophe plurals (Abbreviations.yml)

The URL's must be validated before deployment.
All API's should follow the REST standard.
The JPEG's were uploaded to the CDN.

## Abbreviations — lowercase file types (Abbreviations.yml)

Attach the jpeg file to the request.
Download the pdf from the portal.
The xml output is parsed by the service.
Export the data as a csv file.
Add the png to the assets folder.

## AM/PM format (AMPM.yml)

The maintenance window starts at 9AM and ends at 5PM.
The batch job runs at 2am every night.
The report is generated at 11 a.m. on Fridays.
The backup starts at 12 p.m.

## Undefined acronyms (Acronyms.yml)

The SIEM platform collects log data from all sources.
Configure RBAC to restrict access to sensitive endpoints.
The WAF blocks malicious traffic at the edge.
Use OIDC for federated authentication.

## Acronyms in headings (HeadingAcronyms.yml)

### Using TCP to Connect

### Configuring SSL Certificates

### API Rate Limits
