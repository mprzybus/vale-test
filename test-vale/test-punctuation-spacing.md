# Test: Punctuation and Spacing

Rules covered: Dashes, Ellipses, Hyphens, Auto, Quotes, Spacing, OxfordComma

---

## Dashes — spaces around em dash (Dashes.yml)

The portal — which is available 24/7 — handles all configuration tasks.
This setting — enabled by default — affects all traffic.
The service — launched in 2022 — supports global delivery.

## Ellipses (Ellipses.yml)

Wait for the process to finish...
The configuration is applied...
Loading...

## Hyphens — -ly adverb before hyphen (Hyphens.yml)

Use a highly-available architecture for production workloads.
This is a fully-managed service with no maintenance overhead.
Deploy a globally-distributed network of edge servers.

## Auto- prefix hyphen (Auto.yml)

Enable auto-scaling for the compute tier.
The system supports auto-provisioning of certificates.
Use auto-detection to identify the optimal origin.
Configure auto-failover to improve resilience.

## Quotes — punctuation outside quotes (Quotes.yml)

Set the value to "enabled", then save the configuration.
The response contains the field "status", which indicates success.
Update the "hostname", then activate the rule.

## Spacing — double spaces (Spacing.yml)

Pay attention to this setting.  Another option is available.
The origin server accepts connections.  Configure it carefully.
Update the DNS record.  Wait for propagation to complete.

## Spacing — missing space after period (Spacing.yml)

Check the portal.Then activate the rule.
Save the configuration.Verify the changes in the audit log.
